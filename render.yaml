services:
  - type: web
    name: dmessenger
    runtime: python
    buildCommand: pip install websockets==16.0
    startCommand: python simple_server.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0